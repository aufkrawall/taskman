# Known and Accepted Debt

<!--
TEMPLATE NOTE: Record debt that was deliberately accepted, with the
reasoning, so later audits don't re-derive it and later agents don't "fix"
it without weighing the same trade-off. Items here are recorded, not
endorsed — anything that becomes cheap or safe to fix should be fixed and
removed from this page. Delete this note once real content replaces it.
-->

Last verified: <YYYY-MM-DD>

Primary sources:
- `AGENTS.md`
- `<lint baseline / ratchet file, if any>`
- `<relevant build/test scripts that enforce or track this debt>`

## Purpose

Debt that has been deliberately accepted, with the reasoning, so later
audits do not re-derive it and later agents do not "fix" it without
weighing the same trade-off. Items here are *recorded*, not endorsed.
Anything that becomes cheap or safe to fix should be fixed and removed
from this page.

## `<debt category, e.g. "Oversized modules">`

<What the debt is, why it was accepted, and what would need to be true to
resolve it.>

## Falsified findings — do not re-raise

<!-- Findings from a prior audit/review that were investigated and
confirmed NOT to be real issues. Record them here so a future audit
doesn't spend time re-deriving the same false positive. -->

- <finding> — checked <date>, confirmed not a defect because <reason>.
