* text=auto eol=lf
Cargo.lock linguist-generated=false
# TASKMAN-FORK: upstream tracks *.png / *.gif here through git-LFS, but those objects
# 404 from the upstream LFS server and were never copied into this repo's LFS store.
# The filter rules are disabled so a fresh clone does not try to smudge them, and the
# 230 pointer stubs they used to describe have been removed from this fork's history:
# GitHub rejects any push that carries a pointer whose object it does not have
# (GH008), which made the whole repository unpushable. See TASKMAN-FORK.md.
# `git subtree pull` will re-add them; delete them again in the same commit.
# *.png filter=lfs diff=lfs merge=lfs -text
# *.gif filter=lfs diff=lfs merge=lfs -text

# Exclude some small files from LFS:
crates/eframe/data/* !filter !diff !merge text=auto eol=lf
crates/egui_demo_lib/data/* !filter !diff !merge text=auto eol=lf
crates/egui/assets/* !filter !diff !merge text=auto eol=lf
