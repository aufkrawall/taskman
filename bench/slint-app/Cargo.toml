[package]
name = "bench_slint"
version = "0.1.0"
edition = "2021"

[dependencies]
slint = "1.17"

[build-dependencies]
slint-build = "1.17"

[profile.release]
lto = "thin"
strip = true
codegen-units = 1
